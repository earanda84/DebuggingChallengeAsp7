@{
    ViewData["Title"] = "Generator";
}

<div class="text-center">
    @* Estaba llamando a Locaton y el modelo indica que es Location, además se evalúa con if para validar si existe
    Location, si esta tiene un valor null, se muestre el texto hasta el nombre del usuario, sino que muestre la
    ubicación*@
    @{
        if (Context.Session.GetString("Location") == "Undisclosed")
        {
            <h2>Welcome, @Context.Session.GetString("Username")</h2>
        }
        else
        {
            <h2>Welcome, @Context.Session.GetString("Username") from @Context.Session.GetString("Location")</h2>

        }
    }
    <h2>Here is your randomly generated passcode:</h2>
    <h1 class="passcode">@Context.Session.GetString("Passcode")</h1>
    <div class="d-flex mx-auto w-25 justify-content-center">
        @* Se modifica el action del formulario, solo estaba con action, se entrega la manipulacion al framework con
        asp-action a la ruta Reset *@
        <form asp-action="Reset">
            <label>Not you?</label>
            <input type="submit" value="Log out" class="btn btn-danger">
        </form>
        @* Se modifica el action del formulario, solo estaba con action, se entrega la manipulacion al framework con
        asp-action a la ruta Reset *@
        <form asp-action="GenerateNew" method="post">
            <label>Want a new code?</label>
            <input type="submit" value="New code" class="btn btn-info">
        </form>
    </div>
</div>